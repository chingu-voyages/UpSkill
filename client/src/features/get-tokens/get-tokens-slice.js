// Write all logic to get and use Tokens here
import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  // Initialize state here
};

export const getTokenSlice = createSlice({
  name: "getToken",
  initialState,
  reducers: {
    // Write your different action logics here
  },
});

export const {} = getTokenSlice.actions;

export default getTokenSlice.reducer;
