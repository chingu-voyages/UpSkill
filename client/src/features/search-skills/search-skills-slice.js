// Write all logic to search for a skill or subject here
import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  // Initialize initial state here
};

export const searchSkillsSlice = createSlice({
  name: "searchSkills",
  initialState,
  reducers: {
    // Write your different action logics here
  },
});

export const {} = searchSkillsSlice.actions;

export default searchSkillsSlice.reducer;
