// Write all logic to post tutor reviews here
import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  // Initialize initial state here
};

export const postReviewsSlice = createSlice({
  name: "postReviews",
  initialState,
  reducers: {
    // Write your different action logics here
  },
});

export const {} = postReviewsSlice.actions;

export default postReviewsSlice.reducer;
