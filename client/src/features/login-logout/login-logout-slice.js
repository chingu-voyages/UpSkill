// Write all logic to login and logout here
import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  // Initialize initial state here
};

export const loginLogoutSlice = createSlice({
  name: "loginLogout",
  initialState,
  reducers: {
    // Write your different action logics here
  },
});

export const {} = loginLogoutSlice.actions;

export default loginLogoutSlice.reducer;
