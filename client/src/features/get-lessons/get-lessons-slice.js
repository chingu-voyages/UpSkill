// Write all logic to set up and get all lessons here
import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  // Initialize state here
};

export const getLessonsSlice = createSlice({
  name: "getLessons",
  initialState,
  reducers: {
    // Write your different action logics here
  },
});

export const {} = getLessonsSlice.actions;

export default getLessonsSlice.reducer;
