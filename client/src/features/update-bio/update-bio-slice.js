// Write all logic to update bio info like skills, profile photo, mission statement here
import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  // Initialize initial state here
};

export const updateBioSlice = createSlice({
  name: "updateBio",
  initialState,
  reducers: {
    // Write your different action logics here
  },
});

export const {} = updateBioSlice.actions;

export default updateBioSlice.reducer;
